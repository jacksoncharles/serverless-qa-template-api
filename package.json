{
  "name": "serverless-qa-template-api",
  "version": "1.0.0",
  "description": "Big data, serverless Q&A template API. Written in NodeJS and deployed to AWS. Designed to be implemented as part of a distributed service. Exploits Lambda, API Gateway, Cloudwatch & Dynamodb.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "buildapi": "cd api; npm install"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "bluebird": "^3.5.1"
  },
  "devDependencies": {
    "eslint": "^4.14.0",
    "eslint-config-airbnb-base": "^12.1.0",
    "eslint-plugin-import": "^2.8.0"
  }
}
